[pytest]
markers =
    e2e: end-to-end UI journeys via Selenium
    api: direct API-level tests via requests
    critical: critical-priority scenarios (see docs/test-plan.md)
addopts = -ra --strict-markers
